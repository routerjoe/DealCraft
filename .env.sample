# Red River Sales MCP API Configuration
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.sample .env
# 2. Update values as needed for your environment
# 3. NEVER commit .env to version control
#
# SECURITY NOTE:
# - This file contains NO secrets by design
# - All sensitive data (API keys, tokens) will be added in future sprints
# - Keep .env in .gitignore at all times

# API Configuration
API_HOST=0.0.0.0          # Bind address (0.0.0.0 = all interfaces)
API_PORT=8000             # API server port
API_RELOAD=true           # Auto-reload on code changes (dev only)

# Environment
ENVIRONMENT=development   # Environment name (development/staging/production)

# Logging
LOG_LEVEL=INFO           # Logging verbosity (DEBUG/INFO/WARNING/ERROR/CRITICAL)